<Window x:Class="F1Predictor.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="F1 AI Predictor 2025" Height="720" Width="1280"
        WindowState="Maximized" WindowStartupLocation="CenterScreen">

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16,10" materialDesign:ShadowAssist.ShadowDepth="Depth3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Заголовок ліворуч -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="RacingHelmet" Width="28" Height="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock FontSize="22" FontWeight="Bold" Text="🏁 F1 AI PREDICTOR PRO" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Панель API праворуч -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ProgressBar x:Name="ApiProgressBar" 
                                 Style="{StaticResource MaterialDesignCircularProgressBar}" 
                                 Value="0" IsIndeterminate="True" 
                                 Visibility="Collapsed" 
                                 Width="22" Height="22" Margin="0,0,15,0"
                                 Foreground="White"/>

                    <Button x:Name="BtnUpdateApi" 
                            Click="ButtonUpdateApi_Click" 
                            Style="{StaticResource MaterialDesignOutlinedButton}" 
                            Foreground="White" BorderBrush="White"
                            Height="36" Padding="12,0"
                            materialDesign:ButtonAssist.CornerRadius="18">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CloudRefresh" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Оновити дані API" VerticalAlignment="Center" FontSize="12"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>
        

        <TabControl Grid.Row="1" Style="{StaticResource MaterialDesignFilledTabControl}" TabStripPlacement="Left">
            
            <TabItem Width="80" Height="80">
                <TabItem.Header>
                    <StackPanel>
                        <TextBlock Text="AI" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Прогноз" HorizontalAlignment="Center" FontSize="10"/>
                    </StackPanel>
                </TabItem.Header>

                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="350"/> <ColumnDefinition Width="*"/>   </Grid.ColumnDefinitions>

                    <materialDesign:Card Padding="20" Margin="0,0,20,0" UniformCornerRadius="10" materialDesign:ShadowAssist.ShadowDepth="Depth2" x:Name="SettingsCard">
                        <StackPanel>
                            <TextBlock Text="Налаштування гонки" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,20"/>

                            <ComboBox x:Name="CircuitCombo" materialDesign:HintAssist.Hint="Оберіть трасу 🌍" 
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,20"
                                      DisplayMemberPath="DisplayName" SelectedValuePath="CircuitId"
                                      IsEditable="True" SelectionChanged="CircuitCombo_SelectionChanged"/>

                            <ComboBox x:Name="DriverCombo" materialDesign:HintAssist.Hint="Оберіть пілота 🏎️" 
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,20"
                                      DisplayMemberPath="FullName" SelectedValuePath="DriverId"
                                      IsEditable="True" SelectionChanged="DriverCombo_SelectionChanged"/>

                            <ComboBox x:Name="TeamCombo" materialDesign:HintAssist.Hint="Оберіть команду 🔧" 
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,20"
                                      DisplayMemberPath="Name" SelectedValuePath="ConstructorId"
                                      IsEditable="True" SelectionChanged="TeamCombo_SelectionChanged"/>
                            <StackPanel Margin="0,10,0,10">
                                <TextBlock Text="Режим аналізу:" Foreground="Gray" FontSize="10" Margin="0,0,0,5"/>
                                <DockPanel>
                                    <TextBlock Text="Загальна форма" VerticalAlignment="Center" Foreground="Gray" FontSize="11"/>
        
                                    <ToggleButton x:Name="TrackFilterToggle" Margin="10,0"
                                                  Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                                  ToolTip="Увімкніть, щоб дивитись статистику тільки для цієї траси"
                                                  Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                      
                                    <TextBlock Text="Тільки ця траса" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}" FontWeight="Bold" FontSize="11"/>
                                </DockPanel>
                            </StackPanel>
                            <StackPanel Margin="0,10,0,10">
                            </StackPanel>

                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="Погодні умови:" Foreground="Gray" FontSize="10" Margin="0,0,0,5"/>
                                <DockPanel>
                                    <TextBlock Text="☀️ Сухо" VerticalAlignment="Center" Foreground="#FFEB3B" FontSize="11"/>
        
                                    <ToggleButton x:Name="WeatherToggle" Margin="10,0"
                                                  Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                                  ToolTip="Увімкнути дощ (зменшує надійність, збільшує хаос)"
                                                  Checked="Weather_Changed" Unchecked="Weather_Changed"/>
                      
                                    <TextBlock Text="🌧️ Дощ" VerticalAlignment="Center" Foreground="#2196F3" FontWeight="Bold" FontSize="11"/>
                                </DockPanel>
                            </StackPanel>
                            <TextBlock Text="Стартова позиція (Grid)" Margin="0,10,0,5" Foreground="Gray"/>
                            <DockPanel Margin="0,0,0,30">
                                <TextBlock Text="{Binding ElementName=GridSlider, Path=Value}" DockPanel.Dock="Right" FontWeight="Bold" Margin="10,0,0,0" FontSize="16"/>
                                <Slider x:Name="GridSlider" Minimum="1" Maximum="20" Value="1" TickFrequency="1" IsSnapToTickEnabled="True"/>
                            </DockPanel>
                            <!-- Вибір шин для прогнозу -->
                            <!-- Вибір шин для прогнозу -->
                            <TextBlock Text="Тип шин на старті:" Foreground="Gray" Margin="0,10,0,5"/>
                            <ComboBox x:Name="TyreCombo" SelectedIndex="0" 
                                      Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,15">
                                <ComboBoxItem Content="🔴 Soft (Швидкий старт, +1 піт-стоп)"/>
                                <ComboBoxItem Content="🟡 Medium (Оптимальний баланс)"/>
                                <ComboBoxItem Content="⚪ Hard (Повільний старт, 1 піт-стоп)"/>
                            </ComboBox>

                            <!-- Температура траси -->
                            <TextBlock Text="Температура асфальту (°C):" Foreground="Gray" Margin="0,0,0,5"/>
                            <DockPanel Margin="0,0,0,20">
                                <TextBlock Text="{Binding ElementName=TempSlider, Path=Value, StringFormat={}{0:F0}°C}" 
                                           DockPanel.Dock="Right" FontWeight="Bold" Margin="10,0,0,0" FontSize="14"/>
                                <Slider x:Name="TempSlider" Value="25" Minimum="15" Maximum="55" TickFrequency="1" IsSnapToTickEnabled="True"/>
                            </DockPanel>
                            <Button Height="50" FontSize="18" FontWeight="Bold" Click="ButtonPredict_Click"
                                    materialDesign:ButtonAssist.CornerRadius="25"
                                    Foreground="White" BorderThickness="0">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#FF5252" Offset="0.0"/>
                                        <GradientStop Color="#B71C1C" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Button.Background>

                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FlagCheckered" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBlock Text="ПРОГНОЗ" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            
                            <Button Content="Перевірити точність моделі" Margin="0,20,0,0" Style="{StaticResource MaterialDesignOutlinedButton}" Click="ButtonEvaluate_Click"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" Padding="20" UniformCornerRadius="10" Background="#252525">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                    
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <Border Width="140" Height="140" CornerRadius="70" Margin="0,0,20,0" ClipToBounds="True"
                                                BorderBrush="#444" BorderThickness="2">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="20" ShadowDepth="10" Opacity="0.5" Color="Black"/>
                                            </Border.Effect>
    
                                            <Image x:Name="DriverPhoto" Source="{Binding DriverPhotoPath}" Stretch="UniformToFill"/>
                                        </Border>

                                        <Border Width="100" Height="100" CornerRadius="15" Background="White" VerticalAlignment="Center" Margin="0,0,20,0">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="10" ShadowDepth="5" Opacity="0.3"/>
                                            </Border.Effect>
                                            <Image x:Name="TeamLogo" Source="{Binding TeamLogoPath}" Stretch="Uniform" Margin="10"/>
                                        </Border>
                                        <Border Width="100" Height="100" CornerRadius="10" Background="Transparent" VerticalAlignment="Center">
                                            <Image x:Name="CircuitImage" Source="{Binding CircuitImagePath}" Stretch="Uniform"/>
                                        </Border>
                                    </StackPanel>

                                    <materialDesign:Card Padding="10" Background="#303030" UniformCornerRadius="5">
                                        <StackPanel>
                                            <TextBlock Text="Сер. піт-стоп:" Foreground="Gray" FontSize="10"/>
                                            <TextBlock x:Name="PitStopText" Text="---" FontWeight="Bold" Foreground="#FFD700" FontSize="14"/>
                                        </StackPanel>
                                    </materialDesign:Card>
                                    
                                    <materialDesign:Card Padding="10" Background="#303030" UniformCornerRadius="5">
                                        <StackPanel>
                                            <TextBlock Text="Надійність:" Foreground="Gray" FontSize="10" Margin="0,0,0,5"/>
                                            <Grid>
                                                <ProgressBar x:Name="ReliabilityBar" Style="{StaticResource MaterialDesignCircularProgressBar}"
                                                             Value="0" Maximum="100" Width="40" Height="40" Foreground="#4CAF50"/>
                                                <TextBlock x:Name="ReliabilityText" Text="0%" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" FontWeight="Bold"/>
                                            </Grid>
                                        </StackPanel>
                                    </materialDesign:Card>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Прогнозований результат:" Foreground="Gray" FontSize="16"/>
                                    <TextBlock x:Name="ResultText" Text="{Binding PredictionResultText}" FontSize="48" FontWeight="Bold" Foreground="#FF5252" 
                                               HorizontalAlignment="Left" RenderTransformOrigin="0.5,0.5">
                                        <TextBlock.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        </TextBlock.RenderTransform>
                                    </TextBlock>  
                                </StackPanel>
                            </Grid>
                        </materialDesign:Card>

                        <materialDesign:Card Grid.Row="1" Padding="20" UniformCornerRadius="10">
                            <StackPanel>
                                <TextBlock Text="Історична аналітика (останні виступи)" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,20"/>
                                <lvc:CartesianChart x:Name="StatsChart" Height="300" Margin="10">
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis Title="Місце на фініші" MinValue="1" MaxValue="20" IsMerged="True">
                                            <lvc:Axis.Separator>
                                                <lvc:Separator Step="1"/>
                                            </lvc:Axis.Separator>
                                        </lvc:Axis>
                                    </lvc:CartesianChart.AxisY>
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Останні гонки"></lvc:Axis>
                                    </lvc:CartesianChart.AxisX>
                                </lvc:CartesianChart>
                            </StackPanel>
                        </materialDesign:Card>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Width="80" Height="80" Selector.Selected="HistoryTab_Selected">
                 <TabItem.Header>
                    <StackPanel>
                        <TextBlock Text="DATA" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Історія" HorizontalAlignment="Center" FontSize="10"/>
                    </StackPanel>
                </TabItem.Header>

                <Grid Margin="20">
                    <materialDesign:Card Padding="0" UniformCornerRadius="10">
                        <DataGrid x:Name="HistoryGrid" AutoGenerateColumns="False" IsReadOnly="True" 
                                  CanUserAddRows="False" HeadersVisibility="Column"
                                  materialDesign:DataGridAssist.CellPadding="13 8 8 8" 
                                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM HH:mm}" Width="120"/>
                                <DataGridTextColumn Header="Пілот" Binding="{Binding DriverName}" Width="*"/>
                                <DataGridTextColumn Header="Траса" Binding="{Binding CircuitName}" Width="200"/>
                                <DataGridTextColumn Header="Старт" Binding="{Binding GridPosition}" Width="80"/>
                                <DataGridTextColumn Header="Прогноз" Binding="{Binding PredictedPosition, StringFormat=N1}" Width="100" FontWeight="Bold"/>
                                
                                <DataGridTemplateColumn Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="DEL" Style="{StaticResource MaterialDesignFlatButton}" 
                                                    Foreground="Red" Click="ButtonDelete_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </materialDesign:Card>
                </Grid>
            </TabItem>
            <TabItem Width="80" Height="80">
                <TabItem.Header>
                    <StackPanel>
                        <TextBlock Text="VS" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="Батл" HorizontalAlignment="Center" FontSize="10"/>
                    </StackPanel>
                </TabItem.Header>

                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

                    <materialDesign:Card Padding="20" UniformCornerRadius="10" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <ComboBox x:Name="DriverACombo" materialDesign:HintAssist.Hint="Пілот A (напр. Hamilton)" 
                                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                          DisplayMemberPath="FullName" SelectedValuePath="DriverId"
                                          IsEditable="True"/>
                            </StackPanel>

                            <Button Grid.Column="1" Content="VS" Margin="20,0" Width="60" Height="60"
                                    Style="{StaticResource MaterialDesignFloatingActionDarkButton}"
                                    Click="ButtonCompare_Click" Background="#D32F2F" BorderBrush="#D32F2F"/>

                            <StackPanel Grid.Column="2">
                                <ComboBox x:Name="DriverBCombo" materialDesign:HintAssist.Hint="Пілот B (напр. Schumacher)" 
                                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                          DisplayMemberPath="FullName" SelectedValuePath="DriverId"
                                          IsEditable="True"/>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Row="1" Padding="20" UniformCornerRadius="10">
                        <lvc:CartesianChart x:Name="VsChart" LegendLocation="Bottom">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="Гонки,Перемоги,Подіуми,Поули"></lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis MinValue="0"></lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </materialDesign:Card>
                </Grid>
            </TabItem>
            <!-- 5-ТА ВКЛАДКА: Телеметрія та темп -->
<!-- 5-ТА ВКЛАДКА: Телеметрія та темп -->
<TabItem Width="80" Height="80">
    <TabItem.Header>
        <StackPanel>
            <materialDesign:PackIcon Kind="Speedometer" HorizontalAlignment="Center" Width="24" Height="24"/>
            <TextBlock Text="Темп" HorizontalAlignment="Center" FontSize="10" Margin="0,2,0,0"/>
        </StackPanel>
    </TabItem.Header>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ВЕРХНЯ ПАНЕЛЬ: Картка Індексу Стабільності (Consistency Score) -->
        <materialDesign:Card Grid.Row="0" Padding="15" Margin="0,0,0,15" UniformCornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 1. Найкраще коло -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="⏱ НАЙКРАЩЕ КОЛО" Foreground="Gray" FontSize="11" FontWeight="Bold"/>
                    <TextBlock x:Name="BestLapLabel" Text="--:--.---" FontSize="20" FontWeight="Bold" Foreground="#4CAF50" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- 2. Середній темп -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="📈 СЕРЕДНІЙ ТЕМП" Foreground="Gray" FontSize="11" FontWeight="Bold"/>
                    <TextBlock x:Name="AvgLapLabel" Text="--:--.---" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- 3. Варіативність (StdDev) -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="🎯 ВІДХИЛЕННЯ (StdDev)" Foreground="Gray" FontSize="11" FontWeight="Bold"/>
                    <TextBlock x:Name="StdDevLabel" Text="±0.00 сек" FontSize="20" FontWeight="Bold" Foreground="#FFC107" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- 4. Індекс стабільності -->
                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <TextBlock Text="⭐️ СТАБІЛЬНІСТЬ ПІЛОТА" Foreground="Gray" FontSize="11" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock x:Name="ConsistencyScoreLabel" Text="--%" FontSize="20" FontWeight="Bold" Foreground="#FF5252"/>
                        <ProgressBar x:Name="ConsistencyProgress" Value="0" Maximum="100" Width="60" Height="10" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- НИЖНЯ ПАНЕЛЬ: Картка телеметрії з графіком -->
        <materialDesign:Card Grid.Row="1" Padding="20" UniformCornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Шапка з кнопкою -->
                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Gauge" Width="24" Height="24" Foreground="#FF5252" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="Телеметрія швидкості заїзду (OpenF1 Live)" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                    </StackPanel>
                    
                    <Button x:Name="BtnLoadTelemetry" 
                            Click="ButtonLoadTelemetry_Click" 
                            HorizontalAlignment="Right" 
                            Style="{StaticResource MaterialDesignRaisedButton}" 
                            Background="#FF5252" BorderBrush="#FF5252" Height="36">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CloudDownload" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Завантажити аналітику та телеметрію" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </DockPanel>

                <!-- Контекст даних -->
                <materialDesign:Card Grid.Row="1" Background="#2D2D2D" Padding="12" Margin="0,0,0,15" UniformCornerRadius="5">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Пілот: " Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TelemetryDriverLabel" Text="Не обрано" FontWeight="Bold" Foreground="White" Margin="5,0,20,0" VerticalAlignment="Center"/>
                            
                            <TextBlock Text="Макс. швидкість: " Foreground="Gray" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TelemetryMaxSpeedLabel" Text="--- км/год" FontWeight="Bold" Foreground="#4CAF50" VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="* Показано розгони/гальмування на колі та стабільність темпу" Foreground="Gray" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </DockPanel>
                </materialDesign:Card>

                <!-- Графік -->
                <lvc:CartesianChart Grid.Row="2" x:Name="TelemetryChart" Height="280" LegendLocation="Top">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Точки вимірювання (Telemetry Samples)"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Швидкість (км/год)" MinValue="0" MaxValue="360"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </materialDesign:Card>
    </Grid>
</TabItem>
<!-- НОВА ВКЛАДКА: Стратегія шин -->
<TabItem Width="80" Height="80">
    <TabItem.Header>
        <StackPanel>
            <materialDesign:PackIcon Kind="Tire" HorizontalAlignment="Center" Width="24" Height="24"/>
            <TextBlock Text="Стратегія" HorizontalAlignment="Center" FontSize="10"/>
        </StackPanel>
    </TabItem.Header>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Ліва панель: Налаштування шин та температури -->
        <materialDesign:Card Padding="20" Margin="0,0,20,0" UniformCornerRadius="10" materialDesign:ShadowAssist.ShadowDepth="Depth2">
            <StackPanel>
                <TextBlock Text="Стратегія шин 🛞" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,20"/>

                <TextBlock Text="Тип шин на старті:" Foreground="Gray" Margin="0,0,0,5"/>
                <ComboBox x:Name="TyreStrategyCombo" SelectedIndex="0" 
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,0,15">
                    <ComboBoxItem Content="🔴 Soft (Швидкий старт, +1 піт-стоп)"/>
                    <ComboBoxItem Content="🟡 Medium (Оптимальний баланс)"/>
                    <ComboBoxItem Content="⚪ Hard (Повільний старт, 1 піт-стоп)"/>
                </ComboBox>

                <!-- Температура траси -->
                <TextBlock Text="Температура асфальту (°C):" Foreground="Gray" Margin="0,0,0,5"/>
                <DockPanel Margin="0,0,0,20">
                    <TextBlock x:Name="TempStrategyText" 
                               Text="{Binding ElementName=TempStrategySlider, Path=Value, StringFormat={}{0:F0}°C}" 
                               DockPanel.Dock="Right" FontWeight="Bold" Margin="10,0,0,0" FontSize="14"/>
                    <Slider x:Name="TempStrategySlider" 
                            Value="25" 
                            Minimum="15" 
                            Maximum="55" 
                            TickFrequency="1" 
                            IsSnapToTickEnabled="True"/>
                </DockPanel>

                <!-- 1. Математичний розрахунок (Локальний) -->
                <Button Content="Розрахувати стратегію" 
                        Command="{Binding CalculateTyreStrategyCommand}" 
                        Height="45" FontSize="16" FontWeight="Bold"
                        materialDesign:ButtonAssist.CornerRadius="20"/>

                <!-- 2. НОВА КНОПКА: Реальні дані з OpenF1 API -->
                <Button x:Name="BtnLoadApiStrategy" 
                        Content="Отримати реальні дані шин (OpenF1)" 
                        Click="ButtonLoadApiStrategy_Click" 
                        Height="40" FontSize="13"
                        Margin="0,12,0,0"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        materialDesign:ButtonAssist.CornerRadius="20"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Права панель: Результат та Графік LiveCharts -->
        <materialDesign:Card Grid.Column="1" Padding="20" UniformCornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Аналіз деградації та вікно піт-стопу" Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                    <TextBlock Text="{Binding PitWindowRecommendation}" 
                               FontSize="18" FontWeight="Bold" Foreground="#FF5252" Margin="0,10,0,0"/>
                </StackPanel>

                <lvc:CartesianChart Grid.Row="1" Series="{Binding TyreSeriesCollection}">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Коло гонки" Labels="{Binding LapLabels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Втрата темпу (секунди)"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </materialDesign:Card>
    </Grid>
</TabItem>
        </TabControl>
    </Grid>
</Window>